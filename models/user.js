class User {
    constructor(id, name, email,level, points,password,) {
            this.id = id;
            this.name = name;
            this.email = email;
            this.level = level;
            this.points = points;
            this.password = password;
    }
}

module.exports = User;