class User {
    constructor(id, name, email, password, level) {
            this.id = id;
            this.name = name;
            this.email = email;
            this.password = password;
            this.level = level;
    }
}

module.exports = User;